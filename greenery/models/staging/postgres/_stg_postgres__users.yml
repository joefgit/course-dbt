models:
  - name: stg_postgres__users
    description: base table from postgres representing users in greenery platform
    columns:
      - name: user_guid
        tests:
          - unique
          - not_null