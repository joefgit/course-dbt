models:
  - name: stg_postgres__orders
    description: base table from postgres representing orders in greenery platform
    columns:
      - name: order_guid
        tests:
          - unique
          - not_null